# Local configuration overrides
config.local.sh

# Private overlay: work-specific Applications, workflows and values.
# Merged over git_dir_skel/ at push time and never committed here.
# See examples/private-overlay/ for the layout.
/private/

# Rendered output, regenerated by every setup.sh run
/.render/

# Editor and OS noise
.DS_Store
*.swp
*~
.claude/
